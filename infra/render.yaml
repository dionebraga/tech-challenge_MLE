services:
  - type: web
    name: books-api
    autoDeploy: true  # Corrigido para booleano
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn api.main:app --host 0.0.0.0 --port $PORT"
    plan: free